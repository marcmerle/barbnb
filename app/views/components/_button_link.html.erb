<%#
  Usage:

  <%= render 'components/button_link', text: "ButtonText", link: "link", class: "Classes" % >
%>

<% text = local_assigns.fetch(:text, "Ok") %>
<% link = local_assigns.fetch(:link, 'javascript:;') %>
<% classes = local_assigns.fetch(:class, "") %>

<%= link_to text, link, class: "btn btn-default waves-effect waves-light #{classes}" %>"
